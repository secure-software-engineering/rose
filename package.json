{
  "name": "rose",
  "version": "3.0.1",
  "description": "ROSE is a browser extension researchers can use to capture in situ data on how users actually use online social networks",
  "devDependencies": {
    "del": "^0.1.2",
    "gulp": "^3.8.7",
    "gulp-browserify": "^0.5.0",
    "gulp-shell": "^0.2.9",
    "gulp-uglify": "^0.3.1",
    "adm-zip": "^0.4.4",
    "request": "^2.40.0",
    "run-sequence": "^0.3.6"
  },
  "dependencies": {
    "jquery": "^2.1.1",
    "jsrsasign": "0.0.3",
    "rsvp": "~3.0.6"
  },
  "scripts": {
    "install": "node scripts/downloader.js && cd app/popup && npm install && bower install"
  }
}
