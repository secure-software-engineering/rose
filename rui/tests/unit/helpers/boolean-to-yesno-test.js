import { booleanToYesno } from '../../../helpers/boolean-to-yesno';
import { module, test } from 'qunit';

module('Unit | Helper | boolean to yesno');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = booleanToYesno(42);
  assert.ok(result);
});
